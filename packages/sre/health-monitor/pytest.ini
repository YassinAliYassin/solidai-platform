[pytest]
# The health-monitor tests use async test functions (pytest.mark.asyncio).
# "auto" mode lets them run without an explicit @pytest.mark.asyncio on
# every function, matching how they were written.
asyncio_mode = auto
testpaths = tests
